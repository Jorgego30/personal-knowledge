###### 28/07/2026
### Bandit labs:
	- `tr:` Rotates the letters, needs and argument and a previous phrase or text. Something like this: `cat data.txt | tr ''A-Za-z' 'N-ZA-Mn-za-m''`.
	- `mktemp:` Creates a temporary file or directory in /tmp.
	- `scp:` Sends a file or folder between servers. 
	- `chmod:` Gives or removes permissions of a file. 
		- Flag 600 gives read and write permissions to the user and remove all permissions from group members.
	- `nc (netcat):` Writes or reads from a network using TCP.
	- `openssl:` Toolkit for TLS/SSL protocols, encryption, and certificate management. 
		- `s_client:` Implements a generic SSL/TLS client to connect to a remote host using encrypted connections.
	- `nmap:` Network mapper used for network discovery, port scanning, and service detection.
	- `diff:` Shows differences 
	- `ssh:` If you add a command after the user@ip it will execute on the remote machine. 

password 19 -> 20: 4pIjcunZ0fK2vmp3IwfG8Vf7VhxD6pOA