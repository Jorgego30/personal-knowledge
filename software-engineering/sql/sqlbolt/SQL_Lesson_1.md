### Select queries 101
- `SELECT:` Retrieve data from a SQL database.
- `Query:` Statement which declare what data are we looking for where to find it in the database, and optionally, how to transform it.

Specific columns:
```
	SELECT columns
	FROM mytable;
```

All columns:
```
	SELECT *
	FROM mytable;
```
